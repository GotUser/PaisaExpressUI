import { State } from './state';
/**
 * Settings for ODataString serialization
 */
export interface ODataSettings {
    /**
     * Determines if dates set in filter descriptors should be serialized as UTC,
     * subtracting the offset for the current time zone.
     * The default value is false, meaning that the dates will be serialized without modification.
     */
    utcDates?: boolean;
}
/**
 * Converts a [`State`]({% slug api_kendo-data-query_state %}) into an OData v4 compatible string.
 *
 * @param {State} state - The state that will be serialized.
 * @param {ODataSettings} settings - Settings used during serialization.
 * @returns {string} - The serialized state.
 */
export declare const toODataString: (state: State, settings?: ODataSettings) => string;
